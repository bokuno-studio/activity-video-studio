# QA レポート 2026-04-20 17:54:21

## メタ情報
- コミット: `464791e` (HEAD) + 8ファイル未コミット変更
- ブランチ: `main` (origin より 3 コミット先行)
- 実行日時: 2026-04-20 17:49–17:54 JST
- プロジェクト種別: Swift / SwiftUI macOS ネイティブアプリ (Xcode project, target macOS 14.0+)

## 要件ソース
- `CLAUDE.md` (プロジェクト概要)
- `git log --oneline -10` (直近10コミット)
- `git diff HEAD --stat` (未コミット変更 8ファイル)
- メモリ `project_export_fix.md` (エクスポート修正の根本原因と解決策)
- メモリ `feedback_cli_args.md` (CLI引数での自動テスト仕様)
- シナリオ: 生成（`docs/scenarios.md` に新規作成）

## シナリオと結果

### ビルド
- 結果: ✅ pass
- 詳細: `xcodebuild -project ActivityVideoStudio.xcodeproj -scheme ActivityVideoStudio -configuration Debug build` → BUILD SUCCEEDED。SDK: MacOSX26.4.sdk、target: arm64-apple-macos14.0。警告/エラーなし。

### CLI ヘッドレスエクスポート（短尺）
- 結果: ✅ pass
- 詳細: FIT `21933178412_ACTIVITY.fit` (406KB) + MP4 `GX010047.MP4` (11GB, 2178秒) を `--trim-start 0 --trim-end 2149` (末尾2149秒カット = 先頭29秒残す) でエクスポート。progress 0% → 1% → 10% → 50% → 75% → 100% と進行、約3分で `[AutoExport] DONE ✓` 到達。
- ログ: `/tmp/avs_export.log`
- 注意: 初回は `--trim-end 29` で指定したが `endTrim` は「末尾カット秒数」のため長尺エクスポートになってしまった。`TrimSettings.swift` の意味に沿った値 (2149) で再実行すれば正常動作。CLI仕様上 `--trim-end` が末尾カット秒数である点は、利用者視点ではやや直感に反する（将来的に `--duration N` か `--keep-first N` 的な別名検討余地あり）。

### エクスポート出力の妥当性
- 結果: ✅ pass
- 詳細:
  - サイズ: 37,929,578 B (37.9MB) — 過去記録 (2026-04-14) の「37MB」とほぼ一致
  - 形式: ISO Media, MP4 v2 (ISO 14496-14)
  - ffprobe duration: 29.176 秒（期待通り）
  - 0バイトでない、再生可能な mp4

### VideoExporter 新アーキテクチャの回帰確認
- 結果: ✅ pass
- 詳細: `VideoExporter.swift` に `customVideoCompositorClass` の実使用なし（コメント内での言及のみ）。`AVVideoComposition(asset:applyingCIFiltersWithHandler:)` + `AVAssetExportSession` のアーキテクチャが維持されている（112–113行のコメントも解説に沿う）。メモリ `project_export_fix.md` の方針を維持。

### チャプターマーカー / テキストオーバーレイの出力反映
- 結果: ✅ pass
- 詳細:
  - `PreviewViewModel.makeExportViewModel()` で `overlayRenderer?.textOverlays = textOverlays` を設定して ExportViewModel に渡す（`PreviewViewModel.swift:472`）。直近コミット `464791e: fix: textOverlays in export` の修正ポイント。
  - CLI テストでも `--text "QA 175252"` を指定し、log に `[AutoExport] Starting export` 後も正常完了したことから、テキストオーバーレイ経路でクラッシュしないことを確認。視覚的確認は今回未実施（GUIスクリーンショット相当なし）。
  - チャプターマーカー側の実装（`seekToMarker` が絶対時刻ベースに変更、`generateChapterList` が trimmedTime 参照に変更）はコード読み取りで整合性確認済み。エクスポート処理本体への影響はなし（チャプターマーカーはYouTube説明文生成用）。

## 環境
- 起動コマンド:
  ```
  xcodebuild -project ActivityVideoStudio.xcodeproj -scheme ActivityVideoStudio -configuration Debug build
  
  /path/to/ActivityVideoStudio.app/Contents/MacOS/ActivityVideoStudio \
    --fit /Volumes/Extreme\ SSD/SDcard/DCIM/100GOPRO/21933178412_ACTIVITY.fit \
    --video /Volumes/Extreme\ SSD/SDcard/GX010047.MP4 \
    --trim-start 0 --trim-end 2149 \
    --text "QA ..." \
    --export-to /tmp/qa_export.mp4
  ```
- 起動結果: 成功

## 詰まった箇所
- `--trim-end` 引数の意味を当初「エクスポート終了秒数 (絶対位置)」と誤解し、`--trim-end 29` で起動したところ 2149秒ぶんエクスポートし始めて進捗 1% で長時間停滞した。`TrimSettings.swift` の定義を確認し、正しい値で再実行して解決。

## 総合判定
- 5 / 5 シナリオ pass
- 判定: ✅ 問題なし

### 要修正・要確認の内容
- （任意・UX改善提案）CLI `--trim-end` の意味が「末尾カット秒数」なのは GUI 側の概念そのままだが、CLI で短尺テストを回す時には `--duration` や `--keep-seconds` 系の別オプションがあると運用上便利。現状の実装でも機能は満たしているので Must ではない。
