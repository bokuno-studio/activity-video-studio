# QA レポート 2026-04-20 18:59:00

## メタ情報
- コミット: `86eb1bf` (HEAD, `main`, origin より 5 コミット先行)
  - 直近の機能コミット: `299a449 feat: エクスポートに GPSトラック描画 + UX修正 + CLI debug拡張`
- ブランチ: `main`
- 実行日時: 2026-04-20 18:50–18:59 JST
- プロジェクト種別: Swift / SwiftUI macOS ネイティブアプリ (Xcode project, target macOS 14.0+)

## 要件ソース
- `CLAUDE.md` (プロジェクト概要)
- `docs/scenarios.md` (既存シナリオ — 修正せずそのまま使用)
- `git log --oneline -20`
- 直前の QA レポート `docs/qa/20260420-175421.md`（ベースライン）
- メモリ: `project_export_fix.md`, `feedback_cli_args.md`
- シナリオ: 既存

## シナリオと結果

### 1. ビルド
- 結果: ✅ pass
- 詳細: `xcodebuild -project ActivityVideoStudio.xcodeproj -scheme ActivityVideoStudio -configuration Debug build` → `** BUILD SUCCEEDED **`。SDK: MacOSX26.4.sdk, target: arm64-apple-macos14.0。警告/エラーなし。

### 2. CLI ヘッドレスエクスポート（短尺）
- 結果: ✅ pass
- 詳細: FIT `21933178412_ACTIVITY.fit` + MP4 `GX010047.MP4` (11GB, 2178.2s) を `--trim-start 0 --trim-end 2149 --text "QA 20260420-1851" --export-to /tmp/qa_export.mp4` で実行。
- stdout: progress 6% → 100% で単調増加、約 3〜4 分で `[Export] export finished: status=3 error=none` / `[AutoExport] DONE ✓` 到達。エラーなし。
- ログ: `/tmp/avs_export.log`（並列実行による混在あり。詰まった箇所参照）

### 3. エクスポート出力の妥当性（ffprobe）
- 結果: ✅ pass
- 詳細:
  - サイズ: 37,979,077 B (37.9 MB)
  - 形式: ISO Media, MP4 v2 (ISO 14496-14)
  - 映像: H.264 1920x1080
  - 音声: AAC
  - duration: 29.176000 秒（期待通り）
  - bit_rate: 10,413,785 bps
- ベースライン比較（2026-04-20 17:54 の QA）: 37,929,578 B / 29.176s → 今回 37,979,077 B / 29.176s → 差分 ~50KB（描画タイミングの差程度、許容）。

### 4. VideoExporter 新アーキテクチャの回帰確認
- 結果: ✅ pass
- 詳細: `VideoExporter.swift` 内で `customVideoCompositorClass` の実使用なし（コメント内の説明のみ、28–30 行、112–113 行）。`AVVideoComposition(asset:applyingCIFiltersWithHandler:)` + `AVAssetExportSession` のアーキテクチャが維持されている。メモリ `project_export_fix.md` の方針は `299a449` 後も崩れていない。

### 5. チャプターマーカー / テキストオーバーレイ / GPSトラックの出力反映
- 結果: ✅ pass
- 詳細:
  - `PreviewViewModel.makeExportViewModel()`（`PreviewViewModel.swift:494`）で `overlayRenderer.textOverlays = textOverlays` を設定、ExportViewModel に overlayRenderer を渡す。
  - `OverlayRenderer.swift:215` で `drawGPSTrack(...)` を毎フレーム呼び出し、`OverlayRenderer.swift:218` で textOverlays をフレームに描画。`299a449` で追加された `drawGPSTrack`（329 行〜）は videoSize に対し 22%×28% サイズで右下ではなく右上（上部）に配置。
  - CLI テストで `--text "QA 20260420-1851"` 指定 → 正常完了。GPS トラック描画経路でクラッシュしないことを確認。
  - 視覚的確認（実際にフレームが描画されているか）は今回未実施。次回 QA で ffprobe + `-ss` シーク + スクリーンショット比較を追加検討。

## 環境
- 起動コマンド:
  ```
  xcodebuild -project ActivityVideoStudio.xcodeproj -scheme ActivityVideoStudio -configuration Debug build

  /.../ActivityVideoStudio.app/Contents/MacOS/ActivityVideoStudio \
    --fit "/Volumes/Extreme SSD/SDcard/DCIM/100GOPRO/21933178412_ACTIVITY.fit" \
    --video "/Volumes/Extreme SSD/SDcard/GX010047.MP4" \
    --trim-start 0 --trim-end 2149 \
    --text "QA 20260420-1851" \
    --export-to /tmp/qa_export.mp4
  ```
- 起動結果: 成功

## 詰まった箇所
- **`/tmp/avs_export.log` が並列 QA 実行で混在**: 同時刻に qa-hook トリガーで別の Claude Code インスタンス（peers 確認: ID `ei0lpuhx`, `ge3nqwin`）が同じ QA スキルを実行しており、それぞれが `--export-to /tmp/qa_export.mp4` と `/tmp/avs_export.log` を共有したため、ログファイル内で "progress: 0%" と "progress: X%" が交互に見える現象が発生した。自分自身の background task (`ba9832gxq`) の stdout は単調増加で clean だったため判定には影響なし。
  - 改善案（将来）: CLI の `--export-to` / ログパスに PID か timestamp を混ぜてプロセス間隔離すると、並列 QA でも互いに干渉しない。または qa-hook が QA を 1 本に絞る。

## 総合判定
- 5 / 5 シナリオ pass
- 判定: ✅ 問題なし

### 要修正・要確認の内容
- Must: なし
- Nice-to-have:
  - (既出・`20260420-175421.md` 参照) CLI `--trim-end` の意味が「末尾カット秒数」であり、短尺テスト用には `--duration N` or `--keep-first N` 別名があると運用上便利。
  - 並列 QA 実行のための一時ファイル名のユニーク化（PID/timestamp 付与）。
  - VideoExporter のフレーム描画の視覚的回帰検証（任意フレームを抽出してスクリーンショット比較）を QA 自動化に追加検討。
