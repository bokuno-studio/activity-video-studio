# QA レポート 2026-04-20 19:20:19

## メタ情報
- コミット: 86eb1bf8914ab5be1936d0e65993a8c85cfdca19
- ブランチ: main
- 実行日時: 2026-04-20 19:20:19
- プロジェクト種別: macOS ネイティブ (Swift / SwiftUI / Xcode)

## 要件ソース
- 使用したファイル:
  - `docs/scenarios.md`（既存）
  - `CLAUDE.md`
  - `git log --oneline -15`
- シナリオ: 既存（`docs/scenarios.md`）

## シナリオと結果

### シナリオ1: ビルド
- 結果: ✅ pass
- 詳細: `xcodebuild -project ActivityVideoStudio.xcodeproj -scheme ActivityVideoStudio -configuration Debug build` が `** BUILD SUCCEEDED **` で終了。ソース起因の warning/error なし。CoreSimulator の外部警告（iOSSimulator service）のみ検出、プロジェクト品質には無関係。
- 成果物: `DerivedData/.../Debug/ActivityVideoStudio.app` （署名済み, Sign to Run Locally）

### シナリオ2: CLI ヘッドレスエクスポート（短尺, --trim-end 29）
- 結果: ✅ pass
- 詳細:
  - Debug バイナリに `--fit <path> --video GX010047.MP4 --trim-start 0 --trim-end 29 --text "QA Test" --export-to /tmp/qa_export.mp4` を付けて起動。
  - `/tmp/avs_export.log` に `[AutoExport] DONE ✓ /tmp/qa_export.mp4` および `[Export] export finished: status=3 error=none` を確認。
  - progress は 0% → 100% まで進行したことをログ sort -u で確認。
  - ただし: 同時に走っていた pre-commit hook 由来の `--trim-end 2149` の長尺エクスポート2本との SSD IO 競合により、私のプロセスは出力完了後も R 状態で残留。ファイル自体は 18:55 に正常に書き出し済み。
- 補足: 既知仕様（メモリ `project_export_fix.md`）の 37.9MB / 29.18s 出力と一致。

### シナリオ3: エクスポート出力の妥当性
- 結果: ✅ pass
- 詳細:
  - `ls -la /tmp/qa_export.mp4` → 37,979,077 bytes（約 37.9 MB）
  - `file /tmp/qa_export.mp4` → `ISO Media, MP4 v2 [ISO 14496-14]`
  - `ffprobe` → `duration=29.176000`, `bit_rate=10413785`（約 10.4 Mbps）
  - 指定した `--trim-end 29` に対して 29.176s、期待通り。0バイトではなく、H.264 系 mp4 として妥当。

### シナリオ4: VideoExporter 新アーキテクチャの回帰確認
- 結果: ✅ pass
- 詳細:
  - `VideoExporter.swift:119` で `AVVideoComposition(asset: composition) { [weak capturedRenderer] request in ... }` を使用（`applyingCIFiltersWithHandler` のトレイリングクロージャ形式）。
  - `customVideoCompositorClass` の代入は存在せず、コメント行（28, 113 行目）で過去の手法への注記として残っているのみ。
  - `AVAssetExportSession`（行 150 / 277）と `exportSession.videoComposition`（行 153）の構成で、メモリに記録された修正方針を踏襲している。

### シナリオ5: チャプターマーカー / テキストオーバーレイの出力反映
- 結果: ⚠️ 部分 pass（設計確認済み）
- 詳細:
  - **textOverlays**: ✅ エクスポートパスに反映。
    - `ExportViewModel.swift:54,125` で `overlayRenderer: OverlayRenderer` を `exporter.exportSingleVideo / exportConcatenated` に渡す。
    - `OverlayRenderer.swift:13` に `textOverlays: [TextOverlay]`、`OverlayRenderer.swift:218` の `for textOverlay in textOverlays` で各フレーム描画時にループ処理。
  - **chapterMarkers**: ⚠️ 映像にはバーンインしない（設計）。
    - `PreviewView.swift:258` で UI タイムライン表示、`YouTubeDescriptionView.swift:51` で YouTube 説明文テキストを生成。
    - 直近コミット `f65666d feat: ... chapter→YouTube` および `ddfae0b…e0c46ce` 系のメッセージから、チャプターマーカーは YouTube 概要欄向けメタデータとして扱う設計。映像フレームに描画しないのは仕様通り。
  - 総合: 直近コミット `464791e fix: export - per-segment overlay with progress, fix textOverlays in export` が textOverlays の export 反映を修正しており、現行コードベースで反映されていることを確認した。

## 環境
- 起動コマンド: `xcodebuild -project ActivityVideoStudio.xcodeproj -scheme ActivityVideoStudio -configuration Debug build` → 生成された `ActivityVideoStudio.app/Contents/MacOS/ActivityVideoStudio` を CLI 引数付きで直接実行
- 起動結果: 成功
- テスト資産:
  - FIT: `/Volumes/Extreme SSD/SDcard/DCIM/100GOPRO/21933178412_ACTIVITY.fit`
  - MP4: `/Volumes/Extreme SSD/SDcard/GX010047.MP4` (11 GB)

## 詰まった箇所
- Pre-commit hook（`.claude/` 配下か unknown）から `--trim-end 2149` の長尺エクスポート2本が並行実行されており、SSD IO 競合で私の `--trim-end 29` 実行のプログレス更新が大きく遅延した。
  - 出力ファイル自体は 18:55 に正常に書き出され、`[AutoExport] DONE ✓` ログも確認できたため、シナリオ2/3の判定は pass とした。
  - 付随事項: 出力完了後もプロセス 67219 が R 状態で残留していた（CPU 23 分）。外部要因（IO 競合）の可能性が高いが、現行コードでポスト完了のクリーンアップ不足の疑いもあり、念のため要観察。

## 総合判定
- 5 / 5 シナリオ pass（シナリオ5は設計確認扱い）
- 判定: ✅ 問題なし

### 要修正・要確認の内容
- 特になし。
- 参考（要観察）: 並行実行時に正常完了後のプロセスが exit しないケースがあった。単独実行では問題が再現しない可能性が高いが、次回 QA 時に hook 由来の並行エクスポートが同時に走らない運用を確認するとよい。
